% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/echart-hierarchy-block.R
\name{new_echart_hierarchy_block}
\alias{new_echart_hierarchy_block}
\title{ECharts Hierarchy Filter Block}
\usage{
new_echart_hierarchy_block(
  levels = character(),
  value = character(),
  chart_type = "sunburst",
  title = character(),
  theme = "default",
  ...
)
}
\arguments{
\item{levels}{Character vector. Multi-select for hierarchy columns (order = depth).}

\item{value}{Character. Numeric column for sizing leaf nodes.}

\item{chart_type}{Character. One of "sunburst", "treemap", or "tree".}

\item{title}{Character. Chart title.}

\item{theme}{Character. ECharts theme name.}

\item{...}{Forwarded to \code{\link[blockr.core]{new_transform_block}}}
}
\value{
A blockr transform block that returns filtered data.
}
\description{
A hierarchical filter block that renders sunburst, treemap, or tree charts.
Clicking a node filters the data and passes the filtered data frame
downstream — behaves like a filter block, not a terminal visualization.
}
\examples{
if (interactive()) {
  library(blockr.core)
  accounts <- data.frame(
    asset_class = c("Liquid", "Liquid", "Illiquid", "Illiquid"),
    account = c("Checking", "Savings", "Property A", "Fund X"),
    value = c(50000, 120000, 450000, 200000)
  )
  serve(
    new_echart_hierarchy_block(
      levels = c("asset_class", "account"),
      value = "value"
    ),
    data = list(data = accounts)
  )
}

}
